# autosuggest

Verification of git set-up

changes flow for Pushing
UI Way
1. add(plus button)
2. commit with message
3. push / sync

Command Ways
1. git add -A
2. git commit -m "message name"
3. git push
